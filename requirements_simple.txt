# Simple requirements for the quantitative trading algorithm
# Core dependencies only - no complex multi-language setup

numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0

# Optional: for better performance and additional features
scipy>=1.7.0
scikit-learn>=1.0.0

# Optional: for web interface (if needed later)
flask>=2.0.0
flask-cors>=3.0.0 